Big display metric — Archivo figure, brand-tinted suffix, mono caption. For stat strips.

```jsx
<Stat value="100" suffix="+" label="Proyek Selesai" />
<Stat value="15" suffix="+" label="Sektor Industri" onDark />
```

Use `onDark` over navy/photo backgrounds (suffix becomes lime).
